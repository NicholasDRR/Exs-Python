def aumentar(p, v):
    aumento = p + (p * v / 100)
    return aumento


def dobro(p):
    dob = p * 2
    return dob


def metade(p):
    met = p / 2
    return met

def diminuir(p, taxa):
    diminuida = p - (p * taxa / 100)
    return diminuida
